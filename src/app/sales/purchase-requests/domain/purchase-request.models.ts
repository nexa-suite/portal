export type PurchaseRequestStatus = 'DRAFT' | 'SUBMITTED' | 'IN_REVIEW' | 'NEEDS_ADJUSTMENT' | 'APPROVED' | 'REJECTED' | 'CANCELLED';
export interface PurchaseRequestLine { readonly id: string; readonly catalogItemId: string; readonly itemName: string; readonly presentation: string; readonly quantity: number; readonly unit: string; readonly unitPriceAmount: number; readonly unitPriceCurrency: string; readonly notes: string | null; }
export interface PurchaseRequest { readonly id: string; readonly code: string; readonly status: PurchaseRequestStatus; readonly priority: string; readonly requestedDeliveryDate: string | null; readonly deliveryProfileSnapshot: string | null; readonly paymentOption: string | null; readonly comment: string | null; readonly reviewNote: string | null; readonly lines: readonly PurchaseRequestLine[]; readonly version: number; }
export interface PurchaseRequestPage { readonly items: readonly PurchaseRequest[]; readonly page: number; readonly size: number; readonly total: number; }
export interface PurchaseRequestDraftCommand { readonly priority: string; readonly requestedDeliveryDate: string | null; readonly deliveryProfileSnapshot: string; readonly paymentOption: string; readonly comment: string; readonly lines: readonly { readonly catalogItemId: string; readonly quantity: number; readonly unit: string; readonly notes: string; }[]; }
