# BC-10 — Notifications

Responsable frontend: notificaciones buyer y su estado de lectura.

La raíz contiene las cuatro capas directas `application`, `domain`, `infrastructure` y `presentation`; el shell consume la fachada de este BC mediante composición de la superficie.
