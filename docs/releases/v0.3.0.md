# Nexa Buyer Portal v0.3.0

## Scope

Angular 22 buyer shell at `/home`, packaged as a static Nginx image for the modern runtime.

## Added

- Dockerfile, SPA fallback, healthcheck and Compose port `4300`.
- 50 canonical catalog media assets with checksum validation.
- EN/ES buyer shell and focused unit tests.

## Validation

- `npm run validate:catalog-assets`: 50 assets passed.
- `npm test`: 25 tests passed.
- `npm run build`: passed.
- Playwright smoke at `/home`: served `200`.

## Boundary

No API client, buyer authentication, tenant authorization, persistence or visual parity certification against matched Vue captures is included.
